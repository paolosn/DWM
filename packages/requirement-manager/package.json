{
  "name": "@dwm/requirement-manager",
  "version": "0.1.0",
  "description": "Requerimientos/Trabajos: entidad persistente real que representa un trabajo aceptado de un cliente, vinculada a cliente/perfil/proyecto. Persiste como JSON real en <clientRoot>/REQUERIMIENTOS/<id>.json — sin base de datos, sin duplicar ProjectManager/ProfileManager/ClientManager, solo referencia sus ids reales.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@dwm/core": "*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run --coverage",
    "test:coverage": "vitest run --coverage --coverage.reporter=text --coverage.reporter=html --coverage.reporter=json-summary"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "@vitest/coverage-v8": "^2.0.5",
    "@types/node": "^20.14.10"
  }
}
