{
  "name": "@dwm/skill-manager",
  "version": "0.1.0",
  "description": "Módulo 24 — Skill Manager de Dev Workspace Manager (DWM): gestiona las skills reales detectadas por @dwm/psn-adapter (carpetas del recurso `skills`, cada una con su `SKILL.md` y archivos auxiliares), sin base de datos, sin duplicar información y sin mover ni reorganizar nada salvo lo explícitamente solicitado. Registrable como IModule del Core.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@dwm/core": "*",
    "@dwm/config": "*",
    "@dwm/logger": "*",
    "@dwm/event-bus": "*",
    "@dwm/workspace": "*",
    "@dwm/portable-workspace": "*",
    "@dwm/import-manager": "*",
    "@dwm/psn-adapter": "*",
    "@dwm/verification": "*",
    "@dwm/status": "*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run --coverage",
    "test:coverage": "vitest run --coverage --coverage.reporter=text --coverage.reporter=html --coverage.reporter=json-summary"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "@vitest/coverage-v8": "^2.0.5",
    "@types/node": "^20.14.10"
  }
}
