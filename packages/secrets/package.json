{
  "name": "@dwm/secrets",
  "version": "0.1.0",
  "description": "Gestión segura de secretos de Dev Workspace Manager (DWM). Registrable como IModule del Core; cifrado/descifrado AES-256-GCM, alta/modificación/eliminación, búsqueda por clave, rotación, exportación/importación segura, e integración con @dwm/config, @dwm/logger y @dwm/event-bus.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@dwm/core": "*",
    "@dwm/config": "*",
    "@dwm/logger": "*",
    "@dwm/event-bus": "*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run --coverage",
    "test:coverage": "vitest run --coverage --coverage.reporter=text --coverage.reporter=html --coverage.reporter=json-summary"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "@vitest/coverage-v8": "^2.0.5",
    "@types/node": "^20.14.10"
  }
}
